# Documentation

- `docs/` contains architecture, Grad-CAM, grape/tomato model notes, and Raspberry Pi dependency guidance.
- `reports/` contains retained dataset audits, model reports, integration checks, and preprocessing records.

Some historical reports describe development-time paths or artifacts that were not part of the final repository; they are retained as evidence, not as runtime dependencies.

